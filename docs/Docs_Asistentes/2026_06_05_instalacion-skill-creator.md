# Instalacion de skill-creator

Fecha: 2026-06-05

## Resumen

Se instalo la skill `skill-creator` desde `https://github.com/anthropics/skills` para uso dentro del proyecto.

## Ubicacion

La skill queda ubicada en la carpeta oficial definida por `AGENTS.md`:

```text
skills/skill-creator
```

## Registro

Se actualizo el `Skills Registry` de `AGENTS.md` para referenciar la skill y su ubicacion local.

## Notas

La instalacion inicial del comando genero la skill bajo `.agents/skills/skill-creator`. Esa copia se movio a `skills/skill-creator` y se elimino `.agents` para evitar duplicidad fuera de la estructura oficial del proyecto.

